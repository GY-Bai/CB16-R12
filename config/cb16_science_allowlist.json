{
  "schema": "cb16.science.allowlist.v1",
  "comment": "Repository-owned authority for the Science lane. result_command in a trusted Issue must resolve here; raw shell text is never accepted.",
  "entrypoints": {
    "cb16.noop@v1": {
      "description": "Deterministic no-op science-lane entrypoint. Writes dummy RESULT.json and REPORT.md. Permitted only for dry runs.",
      "argv": [
        "python3",
        "-m",
        "cb16_science.noop"
      ],
      "env": {
        "PYTHONPATH": "science"
      },
      "produces": [
        "RESULT.json",
        "REPORT.md"
      ],
      "dry_run_only": true
    },
    "cb16.smoke@v1": {
      "description": "Bridge smoke-test entrypoint. Proves the Science lane can resolve an allowlisted command, execute it against an exact commit and collect deterministic artifacts. Writes DRY_RUN-labelled artifacts and must never be treated as a scientific result.",
      "argv": [
        "python3",
        "-m",
        "cb16_science.noop"
      ],
      "env": {
        "PYTHONPATH": "science"
      },
      "produces": [
        "RESULT.json",
        "REPORT.md"
      ]
    },
    "cb16.boundary-probe@v1": {
      "description": "Diagnostic-only infra boundary probe. Measures the Science-lane worktree write boundary, the project-store write boundary, and whether a readable /proc/<pid>/environ exposes the dispatcher's CB16_GITHUB_TOKEN key marker to the scrubbed child. Writes RESULT.json and REPORT.md, reports booleans and counts only, and applies no host-side change.",
      "argv": [
        "python3",
        "-m",
        "cb16_science.boundary_probe"
      ],
      "env": {
        "PYTHONPATH": "science"
      },
      "produces": [
        "RESULT.json",
        "REPORT.md"
      ],
      "diagnostic_only": true
    },
    "cb16.runtime-preflight@v1": {
      "description": "Runtime preflight for the Science lane. Reports the CPU Python/NumPy/PyTorch runtime (PyTorch and CUDA are diagnostics only), existence/readability booleans for the canonical /cb16/raw, /cb16/frozen and /cb16/brain_assets inputs advertised by the repository data manifest, the discovered BTCUSDT 2020-01..2020-03 archive count and basenames, and whether one archive opens with a valid OHLCV row. Writes RESULT.json and REPORT.md, emits no environment or credential values, trains nothing.",
      "argv": [
        "python3",
        "-m",
        "cb16_science.runtime_preflight"
      ],
      "env": {
        "PYTHONPATH": "science"
      },
      "produces": [
        "RESULT.json",
        "REPORT.md"
      ],
      "diagnostic_only": true
    },
    "cb16.normalization-ablation@v1": {
      "description": "R12 Normalization Ablation R0. Compares five fixed K-line normalization families (N0 endpoint-anchored log ratios, N1 per-bar log-return geometry, N2 causal window z-score with shared close statistics, N3 RevIN-style per-channel instance normalization, N4 volatility-normalized return geometry) on identical BTCUSDT 1m 2020-01..2020-03 windows, measures the contract invariant probes and one frozen linear sensory projection, and writes experiment_spec.json, RESULT.json and REPORT.md. Trains nothing, performs no Central Brain or Account Physics work, and promotes no winner.",
      "argv": [
        "python3",
        "-m",
        "cb16_science.normalization"
      ],
      "env": {
        "PYTHONPATH": "science"
      },
      "produces": [
        "experiment_spec.json",
        "RESULT.json",
        "REPORT.md"
      ]
    }
  }
}
